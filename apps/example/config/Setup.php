<?php

// App Configurations
return [
	
	// The parent application that the app uses, if applicable.
	// The parent app attempts to load any page that the app cannot.
	// Additionally, any plugin that the parent app uses, the app can also use.
	"Parent App"		=> "Standard"
	
	// Does this application require the use of sessions?
,	"Use Sessions"		=> true
	
	// Does this site require the use of a database?
,	"Use Database"		=> true
	
	// Does this site require a login?
,	"Use Login"			=> true

];

